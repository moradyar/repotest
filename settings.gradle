dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        jcenter()
    }
}

rootProject.name = "RepoTest"
include ':app'
include ':NetworkCore'
include ':RickAndMortyApi'
